# gqlgen-optional-test

Clone with:
```
git clone --recurse-submodules https://github.com/Desuuuu/gqlgen-optional-test.git
```

Run with:
```
go run .
```
After doing modification, re-generate with:

```
go generate ./...
```
